[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: isolated unit tests
    integration: end-to-end tests with mocked external services
    extension: static tests for Chrome extension files
addopts = -ra
